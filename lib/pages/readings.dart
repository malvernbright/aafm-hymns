import 'package:flutter/material.dart';

class Readings extends StatelessWidget {
  const Readings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Column(
          children: [
            Text("Sunday's Readings"),
            Text('Feature coming soon...'),
          ],
        ),
      ),
    );
  }
}
